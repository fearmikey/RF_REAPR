package com.fearmikey.rf_reapr.domain.model

enum class DeviceType {
    GATEWAY,      // Firewall / Router
    SWITCH,
    ACCESS_POINT,
    WORKSTATION,
    SERVER,
    NETWORK_DEVICE,
    MOBILE,
    IOT,
    UNKNOWN
}
